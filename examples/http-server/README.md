# HTTP Server Example

This example will demonstrate the core InferGo value proposition:

- load a supported model artifact
- expose a tiny HTTP endpoint
- run synchronous inference from a normal Go backend service
